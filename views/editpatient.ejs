<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="container">
        <h1>
            <%= title %>
        </h1>
        <form action="/profile" method="POST">
            <div>
                <label>Firstname</label>
                <input type="text" name="first_name" value="<%=editpatient.first_name%>" required>
            </div>
            <div>
                <label>Secondname</label>
                <input type="text" name="second_name" value="<%=editpatient.last_name%>" required>
            </div>
            <div>
                <label>Date of Birth</label>
                <input type="date" name="date_of_birth" value="<%=editpatient.date_of_birth%>" required>
            </div>
            <div>
                <label>Phone</label>
                <input type="tel" name="phone" value="<%=editpatient.phone%>" required>
            </div>
            <div class="form-group">
              <label for="gender">Gender</label>
              <select name="gender" id="gender" required>
                  <option value="Male" <%= editpatient.gender === 'Male' ? 'selected' : '' %>>Male</option>
                  <option value="Female" <%= editpatient.gender === 'Female' ? 'selected' : '' %>>Female</option>
              </select>
          </div>
          
            <div>
                <label>Address</label>
                <input type="text" name="address" value="<%=editpatient.address%>" required>
            </div>
           
            <button type="submit" class="btn btn-primary">Update Patient</button>
        </form>
        <a href="/doctor"><button class="btn btn-secondary">Back to List</button></a>
    </div>
</body>

</html>